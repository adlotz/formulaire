

<div id= "all" classe="parent">
<div id= "testcss">
  <div id= "l1">
    <h2>THP</h2>
    <p> ♫ l'inscription devient plus compliqué</p>
    <p> ♦  _______</p>
    <p> ♣ Avec thp tu peux apprendre plein de nouvelles choses trop stylay</p>
    <p> ♠ Par contre tu va galèrer comme pas possible</p>
    <p> ♥ Mais c'est comme ça que l'on progresse </p>
    
  </div>

<div id = "right">
  <div id= "l2" class="child">
    <h2>Renseignez vos identifiants</h2>
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

  <div class="field">
    <% f.label :email, "Adresse email" %>
    <%= f.email_field :email , autofocus: true, autocomplete: "email", placeholder:"Adresse email...", class: "champ" %>
  </div>  

  <div class="field">
    <% f.label :password, "Mot de passe" %>
    <% if @minimum_password_length %>
    <% end %>
    <%= f.password_field :password, autocomplete: "off", placeholder: "Mot de passe...", class: "champ" %>
  </div>

      <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>

      <div class="actions">
        <%= f.submit "Connection", class: "btn" %>
      </div>

  </div>
    <% end %>
      <div id= "l3">
        <%= link_to "inscription", new_user_registration_path , class: "btn-login" %>
      </div>
      <% end %>     

</div>
</div>
</div>

